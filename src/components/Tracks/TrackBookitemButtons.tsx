function TrackBookitemButtons() {
  return (
    <>
      <button className="text-xs font-semibold text-blue-700 hover:text-blue-800 hover:underline">
        Reading
      </button>
      <button className="text-xs font-semibold text-blue-700 hover:text-blue-800 hover:underline">
        Soon
      </button>
      <button className="text-xs font-semibold text-blue-700 hover:text-blue-800 hover:underline">
        Finished
      </button>
      <button className="text-xs font-semibold text-blue-700 hover:text-blue-800 hover:underline">
        Remove
      </button>
    </>
  );
}

export default TrackBookitemButtons;
